# Requirement

* Git
* NodeJS

# Installation

Make sure you don't have Gulp already installed
-----------------------------------------------

* ```npm install -g gulpjs/gulp-cli electron```
* ```npm install```
* ```gulp```